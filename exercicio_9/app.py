#Crie algumas variáveis com o valor de velocidade;
velocidade_a = 40
velocidade_b = 90
velocidade_c = 120
velocidade_d = 20
velocidade_e = 80
#Crie também uma variável limite com valor de 80;
limite = 80
#Imprima expressões lógicas que verificam se as velocidades são maiores ou iguais a 80;
print(velocidade_a >= limite)
print(velocidade_b >= limite)
print(velocidade_c >= limite)
print(velocidade_d >= limite)
print(velocidade_e >= limite)